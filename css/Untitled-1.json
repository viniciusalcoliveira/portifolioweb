{ 
  "type": "execute",
  "name": "salvarLead",
  "args": {
    "nome": "{{session.nome}}",
    "email": "{{session.email}}",
    "telefone": "{{session.telefone}}",
    "mensagem": "{{session.mensagem}}"
  }
}
